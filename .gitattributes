# Kept out of the dist archive: nobody installing this package needs the test
# suite or the CI config, and every byte here is downloaded by every install.
/.github            export-ignore
/tests              export-ignore
/.gitattributes     export-ignore
/.gitignore         export-ignore
/phpunit.xml        export-ignore

* text=auto eol=lf
